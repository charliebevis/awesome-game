# The Awesomest Game in Elm - [Try It!](http://elm-awesome-game.materialdesignr.com)

## Commands

The `Makefile` contains several commands

* `make` to build `elm.js`
* `make open` to build and open the project in a browser
